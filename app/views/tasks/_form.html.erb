<%= simple_form_for [@project, @task] do |f| %>
  <%= f.input :name, label: 'Enter your task' %>
  <%= f.select :priority, Task::PRIORITIES %>
  <%= f.input :deadline, as: :datetime, label: 'Deadline datetime' %> <!-- , html5: true -->
  <%= f.submit %>

<% end %>