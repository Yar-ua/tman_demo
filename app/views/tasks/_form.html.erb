<!--% if params[:action] == 'edit' %>-->
<!--  %= simple_form_for @task, url: project_task_path(project_id: params[:project_id], id: task.id), method: :put do |f| %>-->
<!--% else %>-->
  <!--%= simple_form_for @task, url: project_tasks_path(project_id: params[:project_id]), method: :post do |f| %>-->
<!--% end %>-->
<%= simple_form_for [@project, @task] do |f| %>
  <%= f.input :name %>
  <%= f.input :deadline, as: :datetime, placeholder: Time.now.strftime('%Y-%m-%d %H:%M'), label: 'Filling date', html5: true %>
  <%= f.submit %>

<% end %>