<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="#"> <%= icon('fas', 'flag') %> Task Manager</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <%= link_to 'Projects', root_path, class: "nav-link" %>
      </li>
      <% if current_user %>
        <li class="nav-item active">
          <a class="nav-link" href="#"><%= current_user.email %> <span class="sr-only">(current)</span></a>
        </li>
        <li class="nav-item">
          <%= link_to 'LogOut', destroy_user_session_path, method: :delete, class: "nav-link" %>
        </li>
      <% else %>
        <%= link_to 'SignIn', new_user_session_path, class: "nav-link" %>
        <%= link_to 'SignUp', new_user_registration_path, class: "nav-link" %>
      <% end %>
    </ul>
  </div>
</nav>